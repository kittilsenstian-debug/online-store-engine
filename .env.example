# Database Configuration (Supabase PostgreSQL)
# Get this from your Supabase project settings > Database > Connection String
# Format: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres

# Redis Configuration (optional but recommended for production)
# For local development, you can use: redis://localhost:6379
# For production, use a Redis service like Upstash, Redis Cloud, or your VPS Redis instance
REDIS_URL=redis://localhost:6379

# Server Configuration
NODE_ENV=development

# CORS Configuration
# For development, you can use: http://localhost:8000,http://localhost:7001
# For production, replace with your actual store and admin URLs
STORE_CORS=http://localhost:8000,http://localhost:7001
ADMIN_CORS=http://localhost:7001,http://localhost:9000
AUTH_CORS=http://localhost:7001,http://localhost:9000

# JWT Secret (generate a secure random string for production)
JWT_SECRET=supersecret

# Cookie Secret (generate a secure random string for production)
COOKIE_SECRET=supersecret
